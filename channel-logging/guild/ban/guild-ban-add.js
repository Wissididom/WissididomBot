const { GuildBan } = require('discord.js');

/**
 * Emitted whenever a member is banned from a guild.
 * @param {GuildBan} ban The ban that occured
 */
async function guildBanAdd(ban) {
    // TODO
}

module.exports.guildBanAdd = guildBanAdd;
