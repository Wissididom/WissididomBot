const { GuildScheduledEvent } = require("discord.js");

/**
 * Emitted whenever a guild scheduled event is created.
 * @param {GuildScheduledEvent} guildScheduledEvent The created guild scheduled event
 */
async function guildScheduledEventCreate(guildScheduledEvent) {
  // TODO
}

module.exports.guildScheduledEventCreate = guildScheduledEventCreate;
