const { GuildScheduledEvent } = require("discord.js");

/**
 * Emitted whenever a guild scheduled event is deleted.
 * @param {GuildScheduledEvent} guildScheduledEvent The deleted guild scheduled event
 */
async function guildScheduledEventDelete(guildScheduledEvent) {
  // TODO
}

module.exports.guildScheduledEventDelete = guildScheduledEventDelete;
