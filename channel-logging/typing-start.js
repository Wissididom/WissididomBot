const { Typing } = require('discord.js');

/**
 * Emitted whenever a user starts typing in a channel.
 * @param {Typing} typing The typing state
 */
async function typingStart(typing) {
    // TODO
}

module.exports.typingStart = typingStart;
