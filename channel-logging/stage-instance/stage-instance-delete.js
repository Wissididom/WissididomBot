const { StageInstance } = require("discord.js");

/**
 * Emitted whenever a stage instance is deleted.
 * @param {StageInstance} stageInstance The deleted stage instance
 */
async function stageInstanceDelete(stageInstance) {
  // TODO
}

module.exports.stageInstanceDelete = stageInstanceDelete;
