const { StageInstance } = require('discord.js');

/**
 * Emitted whenever a stage instance is created.
 * @param {StageInstance} stageInstance The created stage instance
 */
async function stageInstanceCreate(stageInstance) {
    // TODO
}

module.exports.stageInstanceCreate = stageInstanceCreate;
